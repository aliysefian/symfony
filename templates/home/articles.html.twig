{% extends ('base.html.twig') %}
{% block body %}
    <h2>
{#        art form {{ name }}#}

    </h2>
    <div class="row">
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">First</th>
                <th scope="col">Last</th>
                <th scope="col">Handle</th>
            </tr>
            </thead>
            <tbody>

            {% for i in articles %}
                <tr>
                    <th scope="row">{{ i.id }}</th>
                    <td>{{ i.title }}</td>
                    <td><a href="article/{{ i.id }}" class="btn btn-dark"> Show</a></td>
{#                    <td>@mdo</td>#}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
